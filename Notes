1- added Astratomic to Filament but need some testing and add to other pages in Admin panel and other panels 
2- need to make choose lang in Filament interface 


model
public $timestamps = false; 
    
    protected $fillable = [
        'locale',
        'name',
        'description',
    ];
    protected $table = 'employer_translations';


 Schema::create('employer_translations', function (Blueprint $table) {
            $table->id();
            $table->foreignId('employer_id')->constrained()->cascadeOnDelete();
            $table->string('locale');
            $table->string('name')->nullable();
            $table->text('description')->nullable();
            $table->unique(['employer_id', 'locale']);
        });



use Astrotomic\Translatable\Contracts\Translatable as TranslatableContract;
use Astrotomic\Translatable\Translatable;
use App\Models\Employer_translation;

class Employer extends Model implements TranslatableContract

